# WO-000 — Baseline ve freeze

## Amaç
MESA mevcut test/benchmark çıktısının kaydedilmesi, baseline tag oluşturulması, MESA OpenAPI ve capability çıktısının snapshot alınması ve MESA-Law reposunun hazırlanması.

## Ön koşullar
- Çalışan Docker/Docker Compose
- `uv`, `node`, `pnpm`, `python3` yüklü olması
- MESA core profilinin ayakta olması

## İzin verilen repo ve yollar
- `/home/yasin/Desktop/MESA` (sadece okuma ve test çalıştırma)
- `/storage/MESA_Law` (docs klasörü oluşturma)

## Yasak yollar
- MESA source kodunun değiştirilmesi

## Değiştirilecek sözleşmeler
- Yok

## Uygulama adımları
1. Ortamın doğrulanması (docker, uv, node, vs.)
2. `docker compose --profile core up -d` ile core sistemlerin başlatılması
3. `cd /home/yasin/Desktop/MESA` dizininde `git status` ve `git log -1 --oneline` alınması
4. `uv sync --all-extras` çalıştırılması
5. `uv run pytest` çalıştırılarak testlerin çalıştırılıp çıktısının kaydedilmesi
6. Baseline tag oluşturulması (manuel ya da not olarak eklenecek)
7. MESA OpenAPI snapshot alınması
8. `Mesa_Law.md` planının `docs/plans/` altına taşınması

## Kabul kriterleri
- Baseline raporu oluşturuldu mu?
- Tag oluşturuldu mu? (Veya raporlandı mı?)
- Temiz git durumu sağlandı mı?
- Work-order altyapısı (klasörler) kuruldu mu?

## Çalıştırılacak testler
- `uv run pytest` (MESA içinde)

## Güvenlik/veri kontrolü
- MESA reposuna yazma işlemi yapılmamalıdır.

## Rollback
- Gerekirse silinen veya eklenen docs klasörleri kaldırılabilir.

## Teslim çıktıları
- `docs/handoffs/WO-000-HANDOFF.md` dosyası
- `docs/baselines/` altında test ve OpenAPI snapshot raporları
