# WO-000: Baseline ve freeze

## Amaç
MESA repo test durumunu dondurmak ve MESA-Law repo altyapısını başlatmak.

## Ön Koşullar
- `docker`, `docker compose`, `uv`, `node`, `pnpm`, `python3` komutlarının ortamda var olduğu test edildi.

## İzin verilen yollar
- `/home/yasin/Desktop/Storage/MESA_Law/docs/`
- `/home/yasin/Desktop/MESA` (sadece okuma)

## Yasak yollar
- `/home/yasin/Desktop/MESA` içindeki kaynak kodların değiştirilmesi mutlak yasaktır.

## Değiştirilecek sözleşmeler
Yok.

## Uygulama Adımları
1. Gerekli CLI araçlarının versiyon kontrollerinin yapılması.
2. MESA reposunda `uv run pytest` komutunun çalıştırılarak mevcut durumun doğrulanması.
3. MESA-Law projesinde `docs/plans`, `docs/work-orders` ve `docs/handoffs` dizinlerinin oluşturulması.
4. MESA-Law ana planının (Mesa_Law.md) `docs/plans/` altına kopyalanması.

## Kabul Kriterleri
- MESA testlerinin başarıyla geçtiğinin kanıtlanması.
- Master planın yeni klasöre kopyalanması.
- `docs/work-orders/WO-000.md` ve `docs/handoffs/WO-000-HANDOFF.md` belgelerinin yazılması.

## Çalıştırılacak Testler
- `/home/yasin/Desktop/MESA` içinde `uv run pytest`

## Güvenlik - Veri Kontrolü
Veri bütünlüğü MESA üzerinde hiçbir yazma işlemi yapılmayarak sağlandı.

## Rollback
Dosyalar silinebilir.

## Teslim Çıktıları
- Test çıktı logları içeren Handoff dosyası.
